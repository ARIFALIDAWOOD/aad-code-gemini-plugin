{
  "name": "aad-governance-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx scripts/build.ts",
    "build:rules": "tsx scripts/build.ts --rules-only",
    "build:gemini-md": "tsx scripts/generate-gemini-md.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "neverthrow": "^8.2.0",
    "typescript": "^5.8.3",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "@vitest/coverage-v8": "^3.1.1",
    "esbuild": "^0.25.2",
    "tsx": "^4.19.3",
    "vitest": "^3.1.1"
  }
}
